alter table `event_information` drop `category_id`;

alter table `event_information` add `thrust` INT NOT NULL AFTER `event_date`;

ALTER TABLE `event_information` CHANGE `status` `event_status` CHAR(1) CHARACTER SET latin1 COLLATE latin1_swedish_ci NOT NULL;