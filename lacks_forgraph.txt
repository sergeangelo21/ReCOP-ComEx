Admin
	Home
	event thrust= event_information.thrust() 
	event status = event_information.status()
	Users PIE (LInk, Comm, Volun) = user_account.type()
	In stock, Given and Trashed = inventory.status()

	Linkage
	by thrust = user_information.thrust()

	Communities
	distribution of members per community = community.member_count()

	Donations
	New Received and Denied = donation.status()
	
	Inventory
	per item = inventory.type()

	Rating sa reports. meron na igagraph na lang! HAHAHAHA
	